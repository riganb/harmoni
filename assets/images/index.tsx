import { ImageSourcePropType } from "react-native";

const home_bg_artifact =
  require("./home-bg-artifact.png") as ImageSourcePropType;
const home_card_artifact =
  require("./home-card-artifact.png") as ImageSourcePropType;
const doctors_header_artifact =
  require("./doctors-header-artifact.png") as ImageSourcePropType;
const doctor_profile_picture =
  require("./doctor-profile-picture.png") as ImageSourcePropType;

export {
  home_bg_artifact,
  home_card_artifact,
  doctors_header_artifact,
  doctor_profile_picture,
};
